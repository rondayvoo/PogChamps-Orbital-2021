using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "New Weapon Buff/Debuff", menuName = "Item/Abstract/Buffs and Debuffs/Weapon")]
public class BuffsDebuffsWeaponScriptableObject : ItemScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
